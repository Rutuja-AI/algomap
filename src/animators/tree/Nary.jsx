import { TreeNode, TreeEdge } from "./shared";

export default function Nary({ steps, meta }) {
  return (
    <svg width="100%" height="400">
      <text x="50%" y="50%" textAnchor="middle">N-ary Tree Animation (skeleton)</text>
    </svg>
  );
}